import { writeFile } from "node:fs/promises";
import path from "node:path";
import { paths, readJson, rootDir } from "./lib.mjs";

const [
  manifest,
  snapshotIndex,
  changelog,
  sourceAudit,
  sourceAuditLive,
  briefs,
  recordQualityAudit,
  recordQualityReviewerPacket,
  goldV1CertificationStatus,
  reviewDebtLedger,
  externalReviewPacket,
  certificationLedger
] = await Promise.all([
  readJson(paths.manifest),
  readJson(paths.snapshotIndex),
  readJson(paths.changelog),
  readJson(paths.sourceAudit),
  readJson(paths.sourceAuditLive),
  readJson(paths.briefs),
  readJson(paths.recordQualityAudit),
  readJson(paths.recordQualityReviewerPacket),
  readJson(paths.goldV1CertificationStatus),
  readJson(paths.reviewDebtLedger),
  readJson(paths.externalReviewPacket),
  readJson(paths.certificationLedger)
]);

const latestBriefs = [...briefs]
  .sort((a, b) => b.published_date.localeCompare(a.published_date) || a.title.localeCompare(b.title))
  .slice(0, 5);

const previousSnapshot = snapshotIndex.snapshots?.find((snapshot) => snapshot.snapshot_id !== manifest.snapshot_id);

function bullet(label, value) {
  return `- ${label}: ${value}`;
}

const lines = [
  "# Release Notes",
  "",
  `Generated for \`${manifest.snapshot_id}\`.`,
  "",
  "## Snapshot",
  "",
  bullet("Created", `\`${manifest.created_at}\``),
  bullet("Schema version", `\`${manifest.schema_version}\``),
  bullet("Full snapshot hash", `\`${manifest.hashes.full_snapshot}\``),
  bullet("Previous snapshot hash", manifest.hashes.previous_snapshot ? `\`${manifest.hashes.previous_snapshot}\`` : "None"),
  bullet("Archived snapshot", `\`/data/snapshots/${manifest.snapshot_id}.json\``),
  "",
  "## Dataset Counts",
  "",
  bullet("Events", manifest.totals.events),
  bullet("Schools", manifest.totals.schools),
  bullet("Sources", manifest.totals.sources),
  bullet("Briefs", manifest.totals.briefs),
  bullet("Corrections", manifest.totals.corrections),
  bullet("Review queues", manifest.totals.review_queues),
  bullet("Review samples", manifest.totals.review_samples),
  bullet("Review ledger entries", manifest.totals.review_ledger_entries),
  bullet("Methodology examples", manifest.totals.methodology_examples),
  bullet("Workflows", manifest.totals.workflows),
  bullet("Evidence-depth queues", manifest.totals.evidence_depth_queues),
  bullet("Gold record candidates", manifest.totals.gold_record_candidates),
  bullet("Reviewer challenge records", manifest.totals.reviewer_challenge_records),
  bullet("Evidence capsules", manifest.totals.evidence_capsules),
  bullet("Source provenance queues", manifest.totals.source_provenance_queues),
  bullet("Challenge standards", manifest.totals.challenge_standards),
  bullet("Challenge queues", manifest.totals.challenge_queues),
  bullet("Challenge packets", manifest.totals.challenge_packets),
  bullet("Challenge ledger entries", manifest.totals.challenge_ledger_entries),
  bullet("Record quality audit rows", manifest.totals.record_quality_audit_records),
  bullet("Record quality priority rows", manifest.totals.record_quality_audit_priority_records),
  bullet("Record quality reviewer packet queues", manifest.totals.record_quality_reviewer_packet_queues),
  bullet("Gold v1 certified records", manifest.totals.gold_v1_certified_records),
  bullet("Gold v1 blocked records", manifest.totals.gold_v1_blocked_records),
  bullet("Review debt ledger rows", manifest.totals.review_debt_ledger_records),
  bullet("Review debt source families", manifest.totals.review_debt_ledger_source_families),
  bullet("Review debt blocked records", manifest.totals.review_debt_ledger_blocked_records),
  bullet("External review packet records", manifest.totals.external_review_packet_records),
  bullet("External review challenge templates", manifest.totals.external_review_packet_challenge_templates),
  bullet("Certification ledger records", manifest.totals.certification_ledger_records),
  bullet("Certification ledger certified records", manifest.totals.certification_ledger_certified_records),
  bullet("Certification ledger awaiting-review records", manifest.totals.certification_ledger_awaiting_review_records),
  bullet("Certification Batch 001 records", manifest.totals.certification_batch_001_records),
  "",
  "## Dataset Hashes",
  "",
  bullet("Events", `\`${manifest.hashes.events}\``),
  bullet("Schools", `\`${manifest.hashes.schools}\``),
  bullet("Sources", `\`${manifest.hashes.sources}\``),
  bullet("Briefs", `\`${manifest.hashes.briefs}\``),
  bullet("Corrections", `\`${manifest.hashes.corrections}\``),
  bullet("Review log", `\`${manifest.hashes.review_log}\``),
  bullet("Review samples", `\`${manifest.hashes.review_samples}\``),
  bullet("Review ledger", `\`${manifest.hashes.review_ledger}\``),
  bullet("Methodology examples", `\`${manifest.hashes.methodology_examples}\``),
  bullet("Workflows", `\`${manifest.hashes.workflows}\``),
  bullet("Robustness metrics", `\`${manifest.hashes.robustness_metrics}\``),
  bullet("Evidence-depth queues", `\`${manifest.hashes.evidence_depth_queues}\``),
  bullet("Gold record set", `\`${manifest.hashes.gold_record_set}\``),
  bullet("Reviewer challenge pack", `\`${manifest.hashes.reviewer_challenge_pack}\``),
  bullet("Evidence capsules", `\`${manifest.hashes.evidence_capsules}\``),
  bullet("Source provenance queues", `\`${manifest.hashes.source_provenance_queues}\``),
  bullet("Challenge standards", `\`${manifest.hashes.challenge_standards}\``),
  bullet("Challenge queues", `\`${manifest.hashes.challenge_queues}\``),
  bullet("Challenge ledger", `\`${manifest.hashes.challenge_ledger}\``),
  bullet("Record quality audit", `\`${manifest.hashes.record_quality_audit}\``),
  bullet("Record quality reviewer packet", `\`${manifest.hashes.record_quality_reviewer_packet}\``),
  bullet("Gold v1 certification status", `\`${manifest.hashes.gold_v1_certification_status}\``),
  bullet("Review debt ledger", `\`${manifest.hashes.review_debt_ledger}\``),
  bullet("External review packet", `\`${manifest.hashes.external_review_packet}\``),
  bullet("Certification ledger", `\`${manifest.hashes.certification_ledger}\``),
  "",
  "## Evidence Depth & Robustness",
  "",
  "- Robustness dashboard: `/robustness/`",
  "- Robustness metrics: `/data/robustness-metrics.json`",
  "- Evidence-depth queues: `/data/evidence-depth-queues.json`",
  "- Gold record candidates: `/data/gold-record-set.json`",
  "- Reviewer challenge pack: `/data/reviewer-challenge-pack.json`",
  "- Evidence capsules: `/data/evidence-capsules.json`",
  "- Source provenance queues: `/data/source-provenance-queues.json`",
  "- Record quality audit: `/data/record-quality-audit.json`",
  "- Record quality reviewer packet: `/data/record-quality-reviewer-packet.json`",
  "- Gold v1 certification status: `/data/gold-v1-certification-status.json`",
  "- Whole-database review debt dashboard: `/review-debt/`",
  "- Whole-database review debt ledger: `/data/review-debt-ledger.json`",
  "- External review packet: `/external-review/`",
  "- External review packet JSON: `/data/external-review-packet.json`",
  "- Certification ledger: `/certification/`",
  "- Certification ledger JSON: `/data/certification-ledger.json`",
  "- Certification Batch 001 pilot: `/certification/batch-001/`",
  `- Record quality audit current triage: ${recordQualityAudit.totals.blocked_before_external_packet} blocked before external packets, ${recordQualityAudit.totals.needs_internal_review} needing internal review, ${recordQualityAudit.totals.usable_with_review_notes} usable with review notes.`,
  `- Reviewer packet source-link review: ${recordQualityReviewerPacket.source_link_review.hard_broken_sources.length} hard broken sources, ${recordQualityReviewerPacket.source_link_review.locator_risk_sources.length} locator-risk sources.`,
  `- Gold v1 certification gates: ${goldV1CertificationStatus.totals.certified} certified, ${goldV1CertificationStatus.totals.not_certified} not certified, ${goldV1CertificationStatus.totals.blocked} blocked.`,
  `- Review debt ledger current triage: ${reviewDebtLedger.totals.blocked} blocked, ${reviewDebtLedger.totals.high_review_debt} high review debt, ${reviewDebtLedger.totals.medium_review_debt} medium review debt across ${reviewDebtLedger.totals.source_families} source families.`,
  `- External review packet: ${externalReviewPacket.records.length} internally certified Gold v1 records packaged for source-to-record review, with ${externalReviewPacket.challenge_templates.length} challenge templates.`,
  `- Certification ledger: ${certificationLedger.totals.certified} certified, ${certificationLedger.totals.not_certified} not certified, ${certificationLedger.totals.blocked} blocked, and ${certificationLedger.totals.awaiting_review} awaiting review across all ${certificationLedger.totals.records} records.`,
  `- Batch 001 ED dataset pilot: ${certificationLedger.batch_001.records.length} records with ${certificationLedger.batch_001.status_counts.awaiting_review ?? 0} awaiting review because source-to-cell provenance must be explicit before certification.`,
  "- These artifacts describe current dataset composition and review priorities; they must not be used as comparative campus judgments, frequency measures, risk ratings, or approval claims.",
  "",
  "## Adversarial Review Protocol",
  "",
  "- Challenge arena: `/challenge/`",
  "- Challenge standards: `/data/challenge-standards.json`",
  "- Challenge queues and packets: `/data/challenge-queues.json`",
  "- Challenge ledger: `/data/challenge-ledger.json`",
  "- Challenge artifacts are public review workflow aids. They do not represent external audit, endorsement, ranking, safety scoring, severity scoring, prevalence estimates, legal findings, or institutional quality judgments.",
  "",
  "## Public Briefs",
  "",
  ...latestBriefs.map((brief) => `- ${brief.published_date}: [${brief.title}](/briefs/${brief.id}/) (${brief.new_event_ids.length} new, ${brief.updated_event_ids.length} updated)`),
  "",
  "## Changelog",
  "",
  bullet("Public changelog entries", changelog.entry_count),
  bullet("Changelog artifact", "`/data/changelog.json`"),
  "",
  "## Source Audit",
  "",
  bullet("Audit mode", `\`${sourceAudit.mode}\``),
  bullet("Audited sources", sourceAudit.source_count),
  bullet("Referenced events", sourceAudit.event_count),
  bullet("Audit hash", `\`${sourceAudit.audit_hash}\``),
  bullet("Audit artifact", "`/data/source-audit.json`"),
  bullet("Live audit artifact", "`/data/source-audit-live.json`"),
  bullet("Live checked sources", sourceAuditLive.entries?.filter((entry) => entry.launch_check_status === "live_checked").length ?? 0),
  bullet("Live audit hash", `\`${sourceAuditLive.audit_hash}\``),
  "",
  "## Replication",
  "",
  "- Replication packet: `/replicate/`",
  "- Releases artifact: `/data/releases.json`",
  "- Release verification: `/data/release-verification.json`",
  "- Required commands: `npm ci`, `npm run check`, `npm run build`",
  "",
  "## Credibility Boundary",
  "",
  "- Credibility status: `/data/credibility-status.json`",
  "- Public acknowledgments require documented scope and display permission.",
  "- Local verification does not imply outside validation, endorsement, completeness, frequency measurement, or legal truth.",
  "",
  "## Research Exports",
  "",
  "- `/data/events-research.json` and `/data/events-research.csv` join event records with school and source fields.",
  "- `/data/schools-research.json` and `/data/schools-research.csv` add derived event counts, dates, communities, categories, and event IDs.",
  "- `/data/sources-research.json` and `/data/sources-research.csv` add related event IDs, counts, and schools.",
  "",
  "## Prior Snapshot",
  "",
  previousSnapshot
    ? `Previous archived snapshot: \`${previousSnapshot.snapshot_id}\` with full hash \`${previousSnapshot.full_snapshot_hash}\`.`
    : "No prior archived snapshot is currently indexed.",
  "",
  "## Verification",
  "",
  "This release is generated by `npm run release-notes:data` and verified by `npm run build`.",
  ""
];

await writeFile(path.join(rootDir, "RELEASE_NOTES.md"), `${lines.join("\n")}`);

console.log(`Generated release notes for ${manifest.snapshot_id}.`);
